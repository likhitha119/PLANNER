# Server
PORT=5000
NODE_ENV=production

# MongoDB (set one of these)
MONGODB_URI=
# MONGO_URI=

# Auth
JWT_SECRET=

# CORS
CORS_ORIGINS=https://*.vercel.app,http://localhost:5173
CORS_PERMISSIVE=false
